#include <cstdio>
#include <set>
using namespace std;
const int MAXN = 100005;
int N, Q, lo[2][2*MAXN], hi[2][MAXN];
set<int> vals[2];

int main() {
	while (1) {}
	// scanf("%d%d", &N, &Q);
	// for (int r = 0; r < 2; ++r) {
	// 	vals[r].insert(0);
	// 	vals[r].insert(2*N+1);
	// }

	// while (Q--) {
	// 	int cmd; scanf("%d", &cmd);
	// 	if (cmd==1) {
	// 		int r, i, v; scanf("%d%d%d", &r, &i, &v);
	// 		--r;
	// 		lo[r][v] = hi[r][v] = i;
	// 		lo[r^1][v] = v-i;
	// 		hi[r^1][v] = v-i+1;
	// 	} else {
			
	// 	}
	// }
}
